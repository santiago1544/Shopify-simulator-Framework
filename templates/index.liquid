<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Shuffle Store</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="{{ 'styles/main.scss' | asset_url }}" rel="stylesheet">
  </head>
  <body>
  
    {% render 'hero' %}
    {% render 'featured-products' %}
    {% render 'footer' %}
    
    <script src="{{ 'scripts/main.js' | asset_url }}" defer></script>
  </body>
  </html>
  